---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "test-service"
  namespace: "default"
  labels:
    app: "test"
spec:
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 8081
  selector:
    app: "test"
  type: "LoadBalancer"
  loadBalancerIP: ""
